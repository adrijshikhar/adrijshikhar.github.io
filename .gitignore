node_modules
/build
/dist
/*.log
.env
logs
size-plugin.json
.eslintcache
.astro

.idea
*.iml.firecrawl/

# npm lockfile — project uses bun (bun.lock)
package-lock.json

# local agent worktrees / tooling
.claude/*
!.claude/skills/

# designlang extraction output (scratch)
design-extract-output/

# blog-editor review output
scratch/
.playwright-mcp/
.gstack/

# locally-installed skills, not product code
.claude/skills/

# playwright run artifacts
test-results/

# agent scaffolding, not product code
.superpowers/

# planning docs are kept outside this repo
docs/superpowers/

# OS
.DS_Store

# Local tooling config and scratch screenshots — swept in by a `git add -A`
# during the redesign, not project source.
.github/hooks/
rtc-variant-*.png
